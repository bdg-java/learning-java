package com.bdg.Lesson5;

public class App_665 {
    public static void main(String[] args) {
        int n = 5;
        int m = 3;

        char array[] = {'z', 'z', 'b', 'c', 'd', 'o', 'k', 'c', 'z', 'a'};
        char array_one[] = {'t', 'r', 't'};
        int len = array.length;

        for (int i = 0; i < len; i++) {
            for (int k = 0; k < array_one.length; k++) {
                char[] y = new char[array_one.length + len];
                int a = len / 2;

            }

//kisat mnac
        }
    }
}
